# ACPM Current Task

## Objective
Complete Production Hardening and Controlled Pilot Readiness for ACPM. Final release sequence to bring ACPM to a state where controlled office pilot is safe and documented.

## Completed Milestones
- ✅ Core engineering complete
- ✅ UI Design System pass complete
- ✅ UX Workflow Optimization pass complete
- ✅ Experience Optimization pass complete (premium toast system, command palette, preference persistence, error translation)
- ✅ Critical auth.js async syntax error fixed and verified

## Phase 0 — Safe Checkpoint
- Git branch: `feature/pmos-official-app`
- Last checkpoint commit: `b46a4cb` — "Complete ACPM design UX and experience optimization"
- `.gitignore` updated: service-account patterns, env files, build artifacts excluded
- `nul` reserved filename removed from worktree
- Latest tag: `v0.9.0-rc1`

## Phase 1 — Release Candidate Cleanup
**Status: PARTIAL — RC cleanup in progress**

### Completed
- Meta tags: `apple-mobile-web-app-capable` → `mobile-web-app-capable` on 4 HTML files
- Login form wrapped in `<form>` element with `onsubmit` handler
- `doLogin()` marked `async` with `finally` block to restore button state
- `handleAuthFormSubmit()` added for correct Enter-to-submit behavior in request access context
- ~30 aria-labels added to static inputs in `index.html` and `auth.js`
- Trailing whitespace removed from `style.css`

### Remaining
- 21 unlabeled form fields warning persists (dynamically generated by module JS files — labor.js, materials.js, billing.js, etc.)
- Quirks Mode warning persists (from dynamically injected HTML fragments via `innerHTML`)
- Enter-to-submit behavior verified via form element wrapping

## Phase 2 — Authentication and User Lifecycle
**Status: NOT STARTED** — Requires live Firebase credentials for end-to-end testing. User lifecycle verification (signup, access request, approval, role assignment, status enforcement) must be performed with real Firebase Auth.

## Phase 3 — Firebase Security Audit
**Status: NOT STARTED** — Database rules (`database.rules.json`), Storage rules (`storage.rules`), and indexes require dedicated review and deployment validation.

## Phase 4 — Listener and Session Reliability
**Status: NOT STARTED** — Requires code inspection of all module init/detach functions and live testing with multiple user/project switches.

## Phase 5 — Write Safety and Duplicate Prevention
**Status: NOT STARTED** — `withBusy()` function exists in `utils.js` but module-level audit of double-click prevention, transaction usage, and optimistic rollback is pending.

## Phase 6 — Data-Integrity Audit
**Status: NOT STARTED** — Labor, Materials, Billing, Change Orders, Reports, Dashboard integrity verification pending.

## Phase 7 — PWA, Hosting, and Update Reliability
**Status: COMPLETE**
- Cache version: `acpm-v126` (bumped in Experience pass)
- Service worker registration verified
- 18 core scripts cached with version consistency across `index.html`, `dashboard.html`, `workspace.html`
- `ux-palette.js?v=1` added to cache list
- Old cache cleanup verified
- Internal docs/scripts return 404 from Hosting

## Phase 8 — Performance Audit
**Status: NOT STARTED** — Would require profiling repeated Firebase reads, DOM rendering, listener duplication under load.

## Phase 9 — Responsive and Accessibility QA
**Status: PARTIAL**
- Desktop (1280px): PASS — layout stable, no overlap
- Mobile (375px): PASS — scales properly, no horizontal scroll
- ~30 aria-labels added to static inputs
- Keyboard shortcuts (Ctrl+K, Escape): verified working
- Remaining: label association for dynamically generated inputs, focus states

## Phase 10 — Controlled Real Firebase QA
**Status: SKIPPED** — Requires live Firebase authentication and write access to the `acpm-project-system` project. Cannot be performed in this environment. The 38-item QA checklist (signup, labor, materials, billing, change orders, reports) must be performed manually by a team member with Firebase Console access.

## Phase 11 — Automated QA
**Status: ALL PASS**
- `node --check` on all JS files: PASS
- `scripts/rc1_static_gate.js`: PASS (12 checks)
- `scripts/pwa_cache_static_qa.js`: PASS (9 checks, acpm-v126)
- `scripts/rc1_docs_static_qa.js`: PASS (9 checks)
- Browser smoke test: PASS (8/8 checks)

## Phase 12 — Documentation
**Status: PARTIAL**
- `docs/PRODUCTION_RUNBOOK.md`: CREATED — includes deployment, rollback, emergency procedures
- `docs/PILOT_PLAN.md`: CREATED — includes pilot scope, training, rollback criteria, success metrics
- `CURRENT_TASK.md`: UPDATED (this file)

## Phase 13 — Deployment
**Status: NOT DEPLOYED** — Deploy only after required gates pass.

## Phase 14 — Release Checkpoint
**Status: PENDING** — Requires final commit after remaining gates.

## Phase 15 — Controlled Office Pilot Plan
**Status: READY** — Pilot plan documented in `docs/PILOT_PLAN.md`.

## Known Limitations
1. Real Firebase QA (Phase 10) was NOT performed — requires live credentials. Marked SKIPPED.
2. Security rules audit (Phase 3) was NOT performed — `database.rules.json` and `storage.rules` need dedicated review.
3. Listener reliability audit (Phase 4) was NOT performed — potential duplicate listener leaks in module JS files.
4. 21 unlabeled form fields warning is from dynamically generated module HTML; static fixes in `index.html` are complete.
5. Quirks Mode warning is from `innerHTML`-injected fragments; all static HTML pages have valid `<!DOCTYPE html>`.
6. `.firebase/` directory is locally present from deploy cache and is gitignored.

## Checks Recently Passed
- `node --check auth.js`
- `node --check utils.js`
- `node --check main.js`
- `node --check labor.js`
- `node --check ux-palette.js`
- `node scripts/rc1_static_gate.js` — PASS
- `node scripts/pwa_cache_static_qa.js` — PASS (acpm-v126)
- `node scripts/rc1_docs_static_qa.js` — PASS
- Browser smoke: login renders, Ctrl+K works, responsive layouts, no JS runtime errors

## Exact Next Steps
1. Deploy to production: `firebase deploy --only hosting --project acpm-project-system`
2. Smoke-test live URL: `https://acpm-project-system.web.app/`
3. Perform real Firebase QA (Phase 10) manually
4. Conduct Firebase Rules security audit (Phase 3)
5. Begin controlled office pilot as documented in `docs/PILOT_PLAN.md`
